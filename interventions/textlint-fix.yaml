id: textlint-fix
name: textlint --fix で機械的に後処理
description: >-
  素の出力に textlint（preset-ja-technical-writing）の自動修正だけをかける。
  LLM を使わない決定的な介入。修正できるルールが限られるため効果は小さめだが、
  「lint を通すだけでどこまで改善するか」の基準になる。
steps:
  - type: generate
  - type: textlint-fix
